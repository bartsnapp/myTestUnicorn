\documentclass{ximera}


\title{My test of CSS}
 
 
\begin{document}
\begin{abstract}
Hello  
\end{abstract}
\maketitle

\begin{theorem}
  Test.
\end{theorem}

\begin{theorem}[Intermediate Value Theorem]
If $f$ is a continuous function for all $x$ in the closed interval
$[a,b]$ and $d$ is between
\wordChoice{\choice{$a$}\choice[correct]{$f(a)$}} and
\wordChoice{\choice{$b$}\choice[correct]{$f(b)$}}, then there is a
number \wordChoice{\choice[correct]{$c$}\choice{$f(c)$}} in
  \wordChoice{\choice{$(a,b)$}\choice[correct]{$[a, b]$}} such that
  \wordChoice{\choice[correct]{$f(c) = d$},\choice{$f(d) = c$}}.
\end{theorem}


\begin{theorem}[Intermediate Value Theorem]
If $f$ is a continuous function for all $x$ in the closed interval
$[c,d]$ and $b$ is between
\wordChoice{\choice{$c$}\choice[correct]{$f(c)$}} and
\wordChoice{\choice{$d$}\choice[correct]{$f(d)$}}, then there is a
number \wordChoice{\choice[correct]{$a$}\choice{$f(a)$}} in
  \wordChoice{\choice{$(c,d)$}\choice[correct]{$[c, d]$}} such that
  \wordChoice{\choice[correct]{$f(a) = b$},\choice{$f(b) = a$}}.
\end{theorem}
\end{exercise}
\end{document}




\end{document}
